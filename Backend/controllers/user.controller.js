const userModel = require('../models/user.model');
const userServices = require('../services/user.services');
module.exports.userResgister = async (req,res,next)=>{

    // login will comes here next time 28:10
}